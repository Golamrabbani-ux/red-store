import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>
                &copy; {new Date().getFullYear()} Red Store
            </p>
        </footer>
    );
};

export default Footer;